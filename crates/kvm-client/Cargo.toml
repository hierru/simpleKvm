[package]
name = "kvm-client"
version.workspace = true
edition.workspace = true

[dependencies]
kvm-protocol = { path = "../kvm-protocol", features = ["clipboard"] }
eframe = "0.35.0"
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0.151"
tray-icon = "0.24.2"

[target.'cfg(target_os = "macos")'.dependencies]
core-foundation = "0.10"
core-graphics = "0.24"
mdns-sd = "0.20"
