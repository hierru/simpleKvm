# 수정 내역 (Changelog)

날짜별 주요 변경 사항입니다. 괄호 안은 해당 커밋입니다.

## 2026-08-05

### 버그 수정
- **Mac 제어 중 커서가 사라지는 버그 수정** (`b567eda`) — 백그라운드 프로세스가 CGEvent로
  커서를 움직이면 포인터 위치만 이동하고 커서가 다시 그려지지 않는 macOS 동작이 원인.
  Synergy/Barrier가 쓰는 `SetsCursorInBackground` 윈도우 서버 속성을 설정하고, 제어권
  진입 시 `CGDisplayShowCursor`를 호출해 숨김 상태도 해제.
- **Mac→Windows 복귀가 특정 모니터에서만 되는 버그 수정** (`dece84a`) — 기존에는 전체
  배치의 가장 바깥 x를 소유한 디스플레이에서만 복귀가 발동해, ㄱ자(L자) 배치에서 메인
  모니터에서 복귀가 불가능하고 다른 모니터 세로 범위에 대각선으로 걸쳐야만 넘어가는
  문제가 있었음. 이제 어느 디스플레이든 서버 방향 가장자리를 dead zone으로 넘으면 즉시
  복귀 (Synergy 방식).

### 기능 추가
- **모니터 배치 도형(미니맵) 표시** (`b7a18b9`) — Windows 디스플레이 설정 스타일의
  비율 반영 사각형 + 큰 번호. 서버는 Mac으로 전환되는 엣지를 가진 모니터를 파란색으로
  강조, 클라이언트는 메인 디스플레이 강조 + "상세 좌표 보기" 토글. 배치 오탐지 진단용
  `enter:`/`return:` 로그 추가. 개발용 `KVM_ALLOW_SECOND` 환경변수(단일 인스턴스 가드
  우회) 추가.

## 2026-08-03 (밤)

### 기능 추가
- **mDNS 자동 탐색** (`525b2a1`) — 서버가 `_simplekvm._tcp` 서비스를 LAN에 광고하고,
  클라이언트 설정 화면에 "네트워크에서 발견된 서버" 목록 표시 → 클릭 한 번으로 주소·포트
  입력. IP를 몰라도 연결 가능. 실제 광고→탐색 통합 테스트 포함.
- **한/영·한자 키 매핑** (`694587a`) — Windows 한/영 키 → macOS 입력 소스 전환
  (Ctrl+Space), 한자 키 → 한자 변환 (Option+Return). 원샷 단축키 조합(Combo) 매핑
  방식으로 진행 중인 모디파이어 상태는 건드리지 않음.
- **Windows 서버 트레이 아이콘 + 자동 시작 + 상태 색상** (`b8ee4b0`) — 트레이 아이콘
  색으로 상태 표시(회색=중지 / 노랑=대기 / 초록=연결됨), 창을 닫으면 종료 대신 트레이로,
  메뉴에서 열기/자동 실행 토글/종료. `HKCU\...\Run` 레지스트리로 Windows 시작 시
  `--minimized`(트레이 전용) 자동 실행.

## 2026-08-03 (저녁) — GUI 전환

- **양쪽 GUI 앱 전환 + 클립보드 동기화** (`12a01d1`, `84628de`, PR #1) — CLI에서 egui
  기반 GUI로 전환. Mac은 메뉴바(상태표시줄) 앱 + 로그인 시 자동 실행(LaunchAgent) +
  손쉬운 사용 권한 안내 + 앱 번들/서명 스크립트. Windows는 설정/시작·중지/로그 GUI.
  텍스트 클립보드 양방향 동기화 (프로토콜 v2, 에코 억제 포함).
- **서버 단일 인스턴스 가드** (`a56012c`) — 중복 실행 시 안내창 표시 후 종료
  (bind 10048 혼란 방지).
- **macOS 로컬 네트워크 권한 수정** (`ced9cf1`) — 번들 Info.plist에
  `NSLocalNetworkUsageDescription` 추가. macOS 15+에서 이 키가 없으면 권한 팝업 없이
  LAN 연결이 "No route to host"로 조용히 실패하는 문제 해결.
- **진단 문서** (`82930ff`) — 연결 실패 진단 절차 [docs/mac-connection-check.md].
- **엔진 통합 테스트** (`ace39d0`) — 실제 훅 + TCP 핸드셰이크 + 클립보드 양방향 왕복
  검증 (`cargo test -p kvm-server -- --ignored`).

## 2026-08-03 (오후) — 최초 구현

- **MVP** (`ff28d21`) — Rust 워크스페이스 3개 크레이트(protocol/server/client).
  Windows 저수준 훅(WH_MOUSE_LL/WH_KEYBOARD_LL) 캡처 + 엣지 감지, macOS CGEvent 주입,
  TCP + bincode 프로토콜, 다중 모니터(모니터별 좌표 매핑, Windows 가로/세로 회전 포함),
  드래그/더블클릭/모디파이어/스크롤, Ctrl+Alt+F12 비상 복귀, 연결 끊김 시 자동 복귀,
  Mac 없이 서버를 테스트하는 fake_client 예제.
